name: Build and Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security-scan:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Audit dependencies for vulnerabilities
        continue-on-error: true
        run: |
          cargo install cargo-audit --locked
          cargo audit

  build-and-test:
    runs-on: self-hosted
    needs: security-scan
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Rust 
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

            ${{ runner.os }}-target-

      - name: Check formatting
        continue-on-error: true
        run: cargo fmt -- --check

      - name: Run 
        continue-on-error: true
        run: cargo clippy --all-targets --all-features -- -D warnings -D clippy::all

      - name: Build application
        run: cargo build --release --locked

      - name: Generate checksum
        run: |
          cd target/release
          sha256sum portfolio-website > portfolio-website.sha256
          
  deploy:
    runs-on: self-hosted
    needs: build-and-test
    concurrency:
      group: production-deployment
      cancel-in-progress: false
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Verify checksum
        run: |
          cd ./artifact
          sha256sum -c portfolio-website.sha256

      - name: Stop service
        continue-on-error: true
        run: |
          sudo systemctl stop portfolio
          sleep 2

      - name: Deploy application
        run: |
          sudo cp ./artifact/portfolio-website /opt/portfolio/portfolio-website.new
          sudo chmod +x /opt/portfolio/portfolio-website.new
          sudo mv /opt/portfolio/portfolio-website.new /opt/portfolio/portfolio-website
          sudo cp -r src/templates /opt/portfolio/
          sudo cp -r src/static /opt/portfolio/

          sudo chown -R portfolio:portfolio /opt/portfolio
      - name: Start service
        run: |
          sudo systemctl start portfolio
          sleep 5

      - name: Health check
        timeout-minutes: 2
        run: |
          max_attempts=10
          attempt=0
          
          while [ $attempt -lt $max_attempts ]; do
            if curl -f --max-time 5 http://localhost:3000 > /dev/null 2>&1; then
              echo "Health check passed"
              exit 0
            fi
            attempt=$((attempt + 1))
            echo "Health check attempt $attempt/$max_attempts failed, retrying..."
            sleep 3
          done
          
          echo "Health check failed after $max_attempts attempts"
          journalctl -u portfolio -n 50 --no-pager
          exit 1